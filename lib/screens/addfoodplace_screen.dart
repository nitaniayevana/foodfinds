import 'package:flutter/material.dart';

class AddFoodPlaceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Food Place'),
      ),
      body: Center(
        child: Text('Add Food Place Screen'),
      ),
    );
  }
}
